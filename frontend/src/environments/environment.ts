// Ambiente de produção (usado por padrão em `ng build`).
// Ajuste `apiUrl` para a URL real do backend antes de publicar em produção.
export const environment = {
  production: true,
  apiUrl: 'https://meilembretes.onrender.com',
};
